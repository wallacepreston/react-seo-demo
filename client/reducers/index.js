
// ACTION TYPES

// ACTION CREATORS

// THUNK CREATORS

// INITIAL STATE
const initialState = {}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
export default reducer
